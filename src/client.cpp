#include <iostream>
#include <sys/socket.h>
#include <string>
#include <thread>
int main() {
    std::string userName;
    std::getline(std::cin, userName);
    std::cout << userName << std::endl;
    std::cout << "ITS LIKE WHO DID I ASK" << std::endl;

//    int socket = socket(AF_INET, SOCK_STREAM, 0);


    // bind my socket
    //
    // create send and recieve threads
    //

  return 0;
}
